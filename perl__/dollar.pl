#!/usr/bin/perl
use strict;
use warnings;

#the hash-bang command written on the first line indicates that this script
#will be run as a perl script

print "\nThis is the 3rd perl script in the perl tutorial series\n\n";

print "TO USE DOLLAR(\$)in a string\n";
my $value = '$100';
print "My ticket to california cost me $value\n";
